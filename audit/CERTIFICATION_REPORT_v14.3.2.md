# FreightLogic v14.3.2-hardened — Certification Report (Static Bundle)

This report is generated by `red_team_certify_v14_3_1.js` and is intended for **developer handoff** and **release sign-off**.

## Executive Summary

- **Version:** 14.3.2-hardened
- **Certification:** ✅ PRODUCTION-READY
- **Audit type:** Static (no browser execution)

## What’s New in v14.3.2 (Hardening Patch)

### Numeric Hardening Layer
- Introduces `finiteNum`, `posNum`, `intNum` wrappers across input/sanitizer paths.
- Caps:
  - Trip pay: **$1,000,000**
  - Loaded/Empty miles: **300,000**
  - Expense amount: **$1,000,000**
  - Fuel gallons: **100,000**
  - Record IDs: **≤ 1e12** (integer)

### iOS/Safari IndexedDB Self-Heal
- Detects IndexedDB open corruption.
- Performs a one-time per session recovery:
  - deletes DB
  - notifies user
  - reloads

### CSV Parser Optimization
- Fast path for small files.
- Async yielding path for large files (prevents UI freeze).

### Menu Tile XSS Defense-in-Depth
- Escapes tile icon/title/sub using `escapeHtml()`.

## How to Run

From the project folder (same directory as `app.js`):

```bash
node red_team_certify_v14_3_1.js
```

Optional syntax authority check:

```bash
node --check app.js
```

## Notes

- This certification is designed to be **robust and non-brittle**. It avoids regex-based brace counting across template literals and complex regex patterns (common source of false positives).
- ID-sync is best-effort because many UI element IDs are created dynamically; missing IDs are treated as **non-blocking warnings** unless they exceed reasonable thresholds.


## v14.3.2 Add-on: 1‑Tap Navigation

- Added `openTripNavigation(trip)` utility.
- Adds a `Nav` button on each trip row.
- iOS opens Apple Maps; others open Google Maps.
